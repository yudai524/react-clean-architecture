// ============================================================
// {{pascalCase name}}
// ============================================================
export interface I{{pascalCase name}}VM extends {{pascalCase name}} {
  // fetch(): Promise<{Fetch{{pascalCase name}}UseCaseOutput}>
  // create(): Promise<{Create{{pascalCase name}}UseCaseOutput>
  // update(): Promise<{Update{{pascalCase name}}UseCaseOutput}>
  // delete(): Promise<{Delete{{pascalCase name}}UseCaseOutput}>
}
